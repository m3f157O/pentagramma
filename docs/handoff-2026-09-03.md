# Handoff — 2026-09-03 (evening)

## Where we are

### 1. Recovered the bugged session (291d163d)
The old conversation couldn't resume because its context grew to ~238k tokens
(every LLM step hit APITimeoutError). Its content was recovered and continued
in a new session. Nothing was corrupted.

### 2. A4 (Guardian driver validation) — CLOSED
- Driver Verifier soak: **all 4 soak + 7 A1a checks PASS**, no bugchecks.
- Full-corpus drift replay with the final tooling-attribution fixes:
  benign 45→20 / 37→12 / 70→35 (all stay `suspicious`, flap cured);
  tamper re-baselined (70→46; run 3 fixed 27→42/malicious);
  InjectionHarness stable at 90; amsi_detection.ps1 stable at 52.
  No unexpected drift.
- Driver future work is written down in `docs/guardian-driver-future-work.md`
  (A1b: minifilter file protection = the one proven gap, hook-integrity
  verifier; plus optional hardening).

### 3. Interactive browser console — DONE (E2E validated)
Empirical finding that shaped the design: **PSDirect sessions are
non-interactive** (msgbox test `13f0d8cd`: box blocked 90s, never visible).
Probe (`interactive_probe.ps1`): PSRP = session 0, console = session 1;
scheduled task as `gigi`/Interactive/Highest runs in session 1 in 0.6s.

Built and in the working tree:
- `orchestrator/console.py` — ConsoleManager (WMI thumbnail frames ~0.7fps,
  input via persistent PSDirect helper → named pipe → guest input server in
  the interactive session; usage windows for report tagging; open() retries
  the guest-server deploy 3× to absorb the VM-boot PSDirect race that caused
  a PSSessionStateBroken on first open).
- `agent/windows/console_input_server.ps1` — pipe listener (interactive session).
- `scripts/console_session.ps1` — host helper (pipe client over PSSession).
- `scripts/hyperv-vm.ps1` — `Console-InputServer-Start/Stop`,
  `Execute-Sample-Interactive` commands (stdout/stderr read back with
  `[System.IO.File]::ReadAllText` — plain Get-Content ETS-bloats the JSON).
- `agent/windows/interactive_launcher.ps1` — scheduled-task launcher (JSON spec;
  uses `[System.Diagnostics.Process]` — guest `Start-Process -FileName`
  throws in task context; sets `MONITOR_PID_FILE` for traced launches).
- `orchestrator/hyperv.py`, `executor.py` (interactive branch + tagging:
  `interactive_launch` / `interactive_console`), `jobs.py`, `main.py`
  (`/api/console/*` endpoints, `interactive` form field).
- UI: `orchestrator/static/console.html` + `js/console.js`; nav links on all
  pages; dashboard "interactive" checkbox + "Live console →" on running jobs.
- `tests/test_console.py` (10 tests) — full suite **108 passed**.
- Docs updated: `docs/interactive-console-streaming.md` (status: implemented).

Validated live (2026-09-03 night):
- Launcher plain + traced paths via probe 3 (`out/interactive_probe3.ps1`):
  launch_result.json in ~1s, monitor_x64.dll injected into sample pid,
  stdout captured, LastTaskResult 0x0.
- **Full E2E**: msgbox sample with `interactive=true` → VISIBILITY_TEST box
  visible in the browser console, user clicked OK, sample exited on its own
  (`TimedOut: false`, stdout `"box dismissed"`), report carries
  `interactive_launch: true` AND `interactive_console: true`.
- Console open/close, frames streaming at 1024×768, input clicks land.

## Leftovers / notes

### Detection-coverage pack (2026-09-04) — 4 workstreams, all code-complete
- **WS1 NetworkBurstDetected** (EID 9105, heuristics.py): port scan /
  connection flood / DNS flood / DNS-tunnel-suspect. Canary replay 0 drift
  (tamper 42, benign 8, harness 90, amsi 52 unchanged).
- **WS2 .NET static analysis**: parse_dotnet (dnfile) in static_analysis.py,
  obfuscator markers scored +5, UI static tab .NET subsection + capa dotnet
  badge. tests/test_dotnet_static.py (fixture tests/fixtures/hello_dotnet.exe).
- **WS3 YARA-Forge extended**: yara-forge-extended-20260830 vendored
  (10,763 compiled rules vs 5,174 core). scripts/fetch_yara_forge.py new.
  Per-sample drift scan: zero new hits on 92 samples; upstream removed
  COD3NYM_DOTNET_Singlefilehost_Bundled_App → InjectionHarness's next LIVE
  score will drop ~15 (lost static-YARA weight); replays unaffected.
- **WS4 dropped files**: deep re-analysis per retrieved file (analyze()
  run_capa=False + selective capa budget 3) + Sysmon deleted-archive
  retrieval + DroppedFileCapaHit (EID 9106). tests/test_dropped_files.py.
  **Live-validated** (selfclean_dropper.ps1, report c853b4c7): payload
  dropped→ran→deleted was recovered from `C:\SandboxArchive`, hash-verified
  against the FileDelete event, deep-analyzed, downloadable via API.
  Empirical guest facts learned: archive dir has a SYSTEM-only ACL (gigi
  can't even Test-Path) → retrieval stages matched files via a one-shot
  SYSTEM scheduled task (Copy-SandboxArchiveFromVM); archived names are
  `<md5><sha256><ext>` uppercase-hex concatenated; the golden image carries
  **~86k files / 8.5 GB of archive residue** — clean `C:\SandboxArchive`
  before the next SANDBOX_READY refresh.
- Guardian tab added to report UI (sec-guardian, driver alerts + raw events).
- Full suite: 108 pytest + ui_static + 4 new test scripts all pass.
- Throwaway: out\_hello.*, scripts\_parse_*.py, scripts\_replay_canaries.py,
  scripts\_yara_drift_check.py, out\replay_ws1*.txt.
- VM `bande nere` is Off (clean stop after last job).
- The bugged session dir can be archived/deleted if desired:
  `%USERPROFILE%\.kimi\sessions\176ce4d8ccd26ec8748f2c70653eedfe\291d163d-...`
- Uncommitted work: the whole console feature + A4 fixes + coverage pack are
  in the working tree, uncommitted. No commits were made (per policy).
